%table.table.table-sm
  - results.each do |item|
    %tbody
      %tr
        %td.center{style: "width: 100px;"}
          = ResultDecorator.decorate(item).profile_image
        %td
          = ResultDecorator.decorate(item).formatted_info
        %td
          = render 'buttons_actions', user: item
